FORTIO_POD=$(kubectl get pod | grep {{ .Release.Namespace }} | awk '{ print $1 }')
echo $FORTIO_POD

kubectl exec -it $FORTIO_POD -c fortio /usr/local/bin/fortio -- load -curl http://httpbin-default/get
